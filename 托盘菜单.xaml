<Window x:Name="NotfifyIcon" x:Class="托盘菜单"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    ui:ThemeManager.IsThemeAware="True"
    Title="托盘菜单" Height="30" Width="235"
    ResizeMode="NoResize" WindowStyle="None"
    Background="{DynamicResource {x:Static ui:ThemeKeys.AcrylicBackgroundFillColorDefaultBrushKey}}">
    <Grid>
        <Button x:Name="Button_Exit" Content="❌︎退出" HorizontalAlignment="Left" VerticalAlignment="Top" Width="235"/>
    </Grid>
</Window>
